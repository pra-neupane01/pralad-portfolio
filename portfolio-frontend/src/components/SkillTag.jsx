export default function SkillTag({ skill }) {
  return (
    <span className="px-3 py-1 bg-accent/20 border border-accent/50 rounded-full text-sm text-accent font-semibold">
      {skill}
    </span>
  );
}
